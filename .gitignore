# === 数据（自建事实层，不入库）===
/data/*
!/data/.gitkeep
*.db
*.db-wal
*.db-shm
*.db-journal

# === 个人偏好（非项目契约；本仓库公开）===
USER.md

# === Specialist 的脚手架残留 ===
# 运行时**每次 spawn 都会重建**这些文件（不是一次性的，删了还会回来）。
# spawned session 不加载 SOUL.md / IDENTITY.md，所以它们对 Specialist 是死文件；
# 提交进来只会误导后来的人去改一个不生效的地方。
# ⚠️ 只忽略 agents/ 下的 —— 仓库根的那两个是 Supervisor 的，是有效契约。
/agents/*/SOUL.md
/agents/*/IDENTITY.md
/agents/*/BOOTSTRAP.md

# === 运行时 / 密钥 ===
.env
*.key
*.pem
runtime/
node_modules/

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# === Windows 残留 ===
# 从 Windows 复制文件到 WSL 时产生的 NTFS 备用数据流标记
*:Zone.Identifier

# === 临时 ===
*.log
/tmp/
.DS_Store

# 🔴 运行时产物不进仓库（审查第 11 项）
#    上面那十项用 `grep -I` 跳过二进制 ⇒ 压缩包里的内容一个字都扫不到。
#    而仓库是 Public、推即发布。外部评审的原文留在本机 docs/external/ 下即可。
#    ⚠️ 已经进过历史的两个（feishu-design / phase2-code-review）撤不回来 ——
#       它们的内容核过：密钥/家目录/应用标识/邮箱五类全 0 处，不是泄漏。
*.zip
*.tar.gz
*.tgz
*.7z
*.rar

# OpenClaw 自己写的记忆 / 做梦产物。
# 🔴 `.dreams/session-corpus/*.txt` 是**会话逐字记录** ——
#    今天内容无害不代表明天无害，而它是**自动长出来的**，内容不由你决定。
# ⚠️ 已经进过历史的 8 个撤不回来（内容已抽样核过，是行情问答与决策卡）。
memory/

# Claude Code 的本地状态：会话、worktree、锁、个人设置 —— 都不进仓库。
.claude/*
# 🔴 唯一的例外：仓库内的 dev-workflow 技能**必须**在仓库里。
#    它存在的理由是**覆盖用户级同名技能** —— 那一份的 Track D 会去改
#    同机已有实例的 cron 配置，照做直接破坏 I-1。
#    删掉它，任何人克隆后 `/dev-workflow` 就会命中危险的那份。
!.claude/skills/
